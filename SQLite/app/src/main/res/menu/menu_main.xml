<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/action_search"
        app:showAsAction="always"
        android:title="Search"
        app:actionViewClass="androidx.appcompat.widget.SearchView"
        android:icon="@android:drawable/ic_menu_search"></item>
    <item android:id="@+id/action_sort"
        android:title="Sort"
        app:showAsAction="always"
        android:icon="@drawable/ic_sort_white"></item>

    <item android:id="@+id/action_delete_all"
        android:title="Delete All Records"
        app:showAsAction="always"
        android:icon="@drawable/ic_delete_black"></item>

    <item android:id="@+id/action_backup"
        android:title="Backup"></item>

    <item android:id="@+id/action_restore"
        android:title="Restore"></item>
</menu>